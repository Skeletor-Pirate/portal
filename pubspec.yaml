name: academic_architect
description: Academic Architect — School ERP Platform
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.2.0
  postgres: ^3.5.11
  uuid: ^4.5.3
  flutter_markdown: ^0.7.3
  shared_preferences: ^2.5.5
  shimmer: ^3.0.0
  flutter_markdown_latex: ^0.3.4
  share_plus: ^13.1.0
  path_provider: ^2.1.5
  pdf: ^3.12.0
  printing: ^5.14.3
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/avatars/
